# Применяю:
SELECT, FROM, WHERE, LIMIT, OFFSET, IN, BETWEEN, LIKE, COUNT, IS NULL, DISTINCT, агрегатные функции, GROUP BY, ORDER BY, HAVING, JOIN ON(INNER/LEFT/RIGHT/FULL), алиасы(AS),  INSERT/UPDATE/DELETE
